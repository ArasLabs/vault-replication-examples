
The sample shows how to execute manual (and manual ONLY!!!) txns from the standard Innovator client.

In order to install the sample:

1. import the "process_manual_txns" package using imports.mf from the directory
2. copy file "processReplicationTxns.html" into Innovator/Client/scripts directory on server

